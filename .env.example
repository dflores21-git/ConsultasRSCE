# Copia este archivo como ".env" y rellena con tus datos reales.
# NUNCA subas el archivo ".env" (con las credenciales reales) a un repositorio o lo compartas.

DB_USER=admsqlrsce
DB_PASSWORD=MastinEspanol987&
DB_SERVER=RSCEDATOS01
DB_DATABASE=GLOBAL_PRODUCCION

# Puerto en el que arrancará el servidor (opcional, por defecto 3000)
PORT=3000
